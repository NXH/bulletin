<div class = "row">
  <div class = "col-lg-12">
    <div class = "page-header">
      <h1>Your Summary</h1>
    </div>
  </div>
</div>
    <div class = "container" class = "col-lg-12">
      <div class = "row">
        <div class = "col-md-6">
          <h1><img src = "<%= current_account.person1_picture %>" width = "40px" class = "img-circle">  Events for <%= link_to current_account.person1, "/events?person1=#{true}" %></h1>
          <div class = "table-responsive">
            <table class = "table table-striped table-hover">
              <thead>
                <tr>
                  <th>Event</th>
                  <th>Start Time</th>
                  <th>Category</th>
                </tr>
              </thead>
              <tbody>
                <% @events_person1.each do |event| %>
                <tr>
                  <td> <%= event.title %> </td>
                  <td> <%= event.start_time.to_formatted_s(:short) %></td>
                  <td> <%= event.category.name %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
        <div class = "col-md-6">
          <h1><img src = "<%= current_account.person2_picture %>" width = "40px" class = "img-circle">  Events for <%= link_to current_account.person2, "/events?person2=#{true}" %></h1>
          <div class = "table-responsive">
            <table class = "table table-striped table-hover">
              <thead>
                <tr>
                  <th>Event</th>
                  <th>Start Time</th>
                  <th>Category</th>
                </tr>
              </thead>
              <tbody>
                <% @events_person2.each do |event| %>
                <tr>
                  <td> <%= event.title %> </td>
                  <td> <%= event.start_time.to_formatted_s(:short) %></td>
                  <td> <%= event.category.name %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
    <p></p>
    <p></p>
    <div class = "container">
      <div class = "row">
        <div class = "col-md-6">
          <h1><img src = "<%= current_account.person1_picture %>" width = "40px" class = "img-circle">  Tasks for <%= link_to current_account.person1, "/tasks?person1=#{true}" %></h1>
          <div class = "table-responsive">
            <table class = "table table-striped table-hover">
              <thead>
                <tr>
                  <th>Task</th>
                  <th>Due Date</th>
                  <th>Category</th>
                </tr>
              </thead>
              <tbody>
                <% @tasks_person1.each do |task| %>
                <tr>
                  <td> <%= task.title %> </td>
                  <td> <%= task.due_date.to_formatted_s(:short) %></td>
                  <td> <%= task.category.name %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
        <div class = "col-md-6">
          <h1><img src = "<%= current_account.person2_picture %>" width = "40px" class = "img-circle">  Tasks for <%= link_to current_account.person2, "/tasks?person2=#{true}" %></h1>
          <div class = "table-responsive">
            <table class = "table table-striped table-hover">
              <thead>
                <tr>
                  <th>Task</th>
                  <th>Due Date</th>
                  <th>Category</th>
                </tr>
              </thead>
              <tbody>
                <% @tasks_person2.each do |task| %>
                <tr>
                  <td> <%= task.title %> </td>
                  <td> <%= task.due_date.to_formatted_s(:short) %></td>
                  <td> <%= task.category.name %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

